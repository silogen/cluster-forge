services:
  forge:
    image: forge:latest
    container_name: forge
    volumes:
      - $PWD/input:/input
      - $PWD/logs:/logs
      - $PWD/output:/output
      - $PWD/stacks:/stacks
    entrypoint: ["forge"]
