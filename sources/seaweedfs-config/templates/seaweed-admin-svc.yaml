apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: seaweedfs-instance
spec:
  ports:
    - name: http
      port: 80
      targetPort: 23646
      protocol: TCP
  selector:
    app.kubernetes.io/component: admin
    app.kubernetes.io/name: seaweedfs
