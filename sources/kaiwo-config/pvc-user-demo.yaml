apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-user-demo
  namespace: kaiwo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: multinode