# Footprint of the scalable-inference profile

Run `byok/footprint/footprint.sh <label>` on the cluster and paste the output
below. Measure twice: once at idle after the install, and once with the dummy
service running.

This document holds minimal numbers only. There is no baseline from a full
cluster-forge install.

| Item | Value |
|---|---|
| date | not measured yet |
| k0s version | not measured yet |
| VM shape | not measured yet |
| profile | scalable-inference |
| install wall-clock time | not measured yet |

## Idle

Not measured yet.

## With the dummy service

Not measured yet.
