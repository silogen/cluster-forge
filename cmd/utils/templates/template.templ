          ---
          apiVersion: kubernetes.crossplane.io/v1alpha1
          kind: Object
          metadata:
            annotations:
              gotemplating.fn.crossplane.io/composition-resource-name: {{ .Kind }}
          spec:
            providerConfigRef:
              name: kubernetes-provider
            forProvider:
              manifest:
{{ .Content }}