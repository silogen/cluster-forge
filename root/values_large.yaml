enabledApps:
  - ai-gateway-discovery
  - aim-cluster-model-source
  - aim-engine
  - aim-engine-crds
  - airm
  - airm-infra-cnpg
  - airm-infra-external-secrets
  - airm-infra-rabbitmq
  - aiwb
  - aiwb-infra-cnpg
  - aiwb-infra-external-secrets
  - amd-gpu-operator
  - amd-gpu-operator-config
  - appwrapper
  - argocd
  - argocd-config
  - cert-manager
  - cnpg-operator
  - external-secrets
  - external-secrets-config
  - inference-extension-crds
  - envoy-gateway
  - envoy-gateway-config
  - envoy-ai-gateway-crds
  - envoy-ai-gateway
  - gitea
  - gitea-config
  - kaiwo
  - kaiwo-config
  - kaiwo-crds
  - keda
  - kedify-otel
  - keycloak
  - kserve
  - kserve-crds
  - kueue
  - kueue-config
  - kuberay-operator
  - kyverno
  - kyverno-policies-base
  - metallb
  - seaweedfs-crds
  - seaweedfs-operator
  - seaweedfs-config
  - openbao
  - openbao-config
  - opentelemetry-operator
  - otel-lgtm-stack
  - prometheus-crds
  - rabbitmq
  
apps:
  airm-infra-cnpg:
    valuesObject:
      instances: 2
  keycloak:
    valuesObject:
      cnpg:
        instances: 2
  seaweedfs-config:
    valuesObject:
      seaweed:
        storageClassName: mlstorage
        master:
          replicas: 3
          volumeSizeLimitMB: 1024
        volume:
          replicas: 3
          requests:
            storage: 500Gi
        filer:
          replicas: 1
          requests:
            storage: 30Gi
      bucket:
        name: default-bucket
        namespace: demo

  # Cluster size tier: LARGE -- multi-node clusters (3 control planes, 3-20 nodes).
  #
  # Only two components scale with cluster size (EAI-8713 measurements,
  # 2026-09-15/16): lgtm memory, and the k8s metrics collector, both driven by
  # control-plane count rather than node count. Every other container measured
  # flat across all seven clusters and therefore inherits the chart default,
  # which is the small/medium tier.
  otel-lgtm-stack:
    valuesObject:
      lgtm:
        # Measured 3.0-5.6 GiB on 3-CP clusters running v1.0.8. Clusters on
        # v1.0.7 read higher (~9 GiB) but have no retention, so those numbers
        # are not a valid sizing input. Some clusters already run a 16Gi limit
        # as a per-cluster patch -- this makes it the tier default.
        resources:
          limits:
            memory: 16Gi
          requests:
            cpu: 750m
            memory: 4Gi
        storage:
          # 100Gi rather than small/medium's 50Gi. Retention (168h) is what
          # actually bounds Loki, but large clusters ingest more and this is
          # the PVC that has needed expanding in the past -- and PVCs can be
          # expanded, never shrunk, so the cost of starting too small is a
          # manual migration.
          loki: 100Gi
      collectors:
        resources:
          # otel-collector-metrics-k8s. Measured 0.9-2.2 GiB. Raised to 16Gi
          # because this collector has OOMKilled at 8Gi and the spike
          # is still unexplained; large clusters have the capacity to absorb it.
          metrics:
            limits:
              memory: 16Gi
            requests:
              cpu: 750m
              memory: 4Gi
  openbao:
    valuesObject:
      server:
        ha:
          enabled: true
          raft:
            enabled: true
          replicas: 3
