# Project-specific OpenBao configuration
# This file can be customized for specific deployment needs

# Override default values as needed
server:
  # Example: Use specific storage class
  dataStorage:
    storageClass: "default"  # Adjust based on available storage classes

  auditStorage:
    storageClass: "default"  # Adjust based on available storage classes

  # Example: Configure specific resource limits for production
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "2Gi"
      cpu: "1000m"

  # Example: Enable ingress if needed
  # ingress:
  #   enabled: true
  #   annotations:
  #     kubernetes.io/ingress.class: nginx
  #     cert-manager.io/cluster-issuer: letsencrypt-prod
  #   hosts:
  #     - host: vault.example.com
  #       paths:
  #         - path: /
  #           pathType: Prefix
  #   tls:
  #     - secretName: vault-tls
  #       hosts:
  #         - vault.example.com

# Example: Additional environment variables
# server:
#   extraEnvironmentVars:
#     VAULT_LOG_LEVEL: info
#     VAULT_LOG_FORMAT: json
