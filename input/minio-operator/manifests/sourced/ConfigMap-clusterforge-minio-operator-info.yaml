---
apiVersion: v1
kind: ConfigMap
metadata:
  name: clusterforge-minio-operator-info
  namespace: minio-operator
data:
  source.yaml: |
    name: minio-operator
    namespace: "minio-operator"
    helm-chart-name: "operator"
    helm-name: minio-operator
    helm-url: "https://operator.min.io"
    helm-version: "7.0.0"
    values: minio-values.yaml
    
