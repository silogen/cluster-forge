---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kgateway-cert
  namespace: kgateway-system
spec:
  dnsNames:
    - '*'
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: kgateway-cert
