# metallb-system/memberlist
apiVersion: v1
kind: Secret
metadata:
  name: memberlist
  namespace: metallb-system
type: Opaque
stringData:
  secretkey: placeholder

---
# workbench/minio-credentials (standalone mode only)
# Note: Named "minio-credentials" for backward compatibility, used with SeaweedFS S3
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: workbench
type: Opaque
stringData:
  minio-access-key: placeholder  # Key name kept for backward compatibility
  minio-secret-key: placeholder  # Key name kept for backward compatibility